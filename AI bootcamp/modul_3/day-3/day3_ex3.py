.import numpy as np 

# Define the gradient descent function
def gradient_descent(X, y,theta, leatning_rate, iterations):
    m = len(y)
    for _ in range(iterations):
        predictions = np.dot
    